import React from "react";

const Subtitulo = ({nombre}) => {
  return <p className="lg:text-3xl md:text-3xl sm:text-xl mt-10 font-semibold dark:text-white">{nombre}</p>;
};

export default Subtitulo;
